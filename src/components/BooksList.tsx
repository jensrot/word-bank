import React from "react";
import { ScrollView, Text } from "react-native";

export default function BooksList() {
    return (
        <ScrollView>
            <Text>All books</Text>
        </ScrollView>
    );
}